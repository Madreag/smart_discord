[project]
name = "smart-discord-shared"
version = "0.1.0"
description = "Shared Pydantic models and utilities"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.9.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"
